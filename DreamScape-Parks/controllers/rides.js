const { Post } = require('../models')

const GetRides = async (req, res) => {
  console.log(req)
}

const CreateRides = async (req, res) => {
  console.log(req)
}

module.exports = {
  GetRides,
  CreateRides
}
